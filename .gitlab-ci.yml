image: docker:19.03.4-dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""


stages:
  - build


Build Django App:
  stage: build
  tags:
    - docker
  services:
    - docker:19.03.4-dind
  variables:
    IMAGE_TAG: $CI_REGISTRY_IMAGE-django:$CI_COMMIT_SHA
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build --pull -t $IMAGE_TAG .
    - docker push $IMAGE_TAG

Build Nginx:
  stage: build
  tags:
    - docker
  services:
    - docker:19.03.4-dind
  variables:
    IMAGE_TAG: $CI_REGISTRY_IMAGE-nginx:$CI_COMMIT_SHA
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build --pull -t $IMAGE_TAG nginx/
    - docker push $IMAGE_TAG

Build RTMP Ingest:
  stage: build
  tags:
    - docker
  services:
    - docker:19.03.4-dind
  variables:
    IMAGE_TAG: $CI_REGISTRY_IMAGE-ingest:$CI_COMMIT_SHA
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build --pull -t $IMAGE_TAG .
    - docker push $IMAGE_TAG